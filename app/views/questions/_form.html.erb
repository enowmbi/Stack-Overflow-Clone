<%= render "layouts/errors", :object => @post %>

<%= bootstrap_form_for @question do |f| %>
  <div class="form-group">
    <%= f.text_area :question, :rows => 12, :cols => 75  %>
  </div>
  <div class="form-group">
    <%= f.submit "Submit Your Question" %>
  </div>
<% end %>
